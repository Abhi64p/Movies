import React from 'react';
import HomeScreen from './screens/HomeScreen';

/**
 * No navigation libraries are used as there is only one screen to implement.
 */
const App = () => <HomeScreen />;

export default App;
